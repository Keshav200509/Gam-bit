package com.example.domain.model

enum class GameLevel(val level: Int, val displayName: String) {
    LEVEL_1(1, "Level 1 — Accessible"),
    LEVEL_2(2, "Level 2 — Standard"),
    LEVEL_3(3, "Level 3 — Expert")
}
